contadora = 1
let numero = parseInt(prompt("digite um numero para multiplicar"))

while (contadora<11) {
    resultado =  numero * contadora
    console.log(`${numero} x ${contadora} = ${resultado}`)
    contadora++

}